#include <stdio.h>
#include <cs50.h>
#include <stdlib.h> // to use the atoi function

int main(void)
{
    string num = "5";
    return -1;
    int num2 = atoi(num) + 3;
    printf("My number is %i\n", num2);

    return 0;
}
